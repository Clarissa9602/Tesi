using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class MessageGenerator : MonoBehaviour{
   /* public ObjectPooler messagePool;

    // Set distance between messages
    public float distanceBetweenMessage;
    public void SpawnMessage(Vector3 startPosition){
        // Spawn message to center side
        GameObject message1 = messagePool.GetPooledObject();
        message1.transform.position = startPosition;
        message1.SetActive(true);
        
        // Spawn message to left side
        GameObject message2 = messagePool.GetPooledObject();
        message2.transform.position = new Vector3(startPosition.x, startPosition.y, startPosition.z - distanceBetweenMessage);
        message2.SetActive(true);

        // Spawn message to right side
        GameObject message3 = messagePool.GetPooledObject();
        message3.transform.position = new Vector3(startPosition.x, startPosition.y, startPosition.z + distanceBetweenMessage);
        message3.SetActive(true);
    }

    */
}
